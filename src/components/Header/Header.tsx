import styles from './Header.module.css';

const Header = () => (
  <header className={styles.header}>
    <h1 className={styles.headerHeading}>create a recipe card</h1>
  </header>
);

export default Header;
